apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mlappdeploy.fullname" . }}-test-connection"
  labels:
    {{- include "mlappdeploy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mlappdeploy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
