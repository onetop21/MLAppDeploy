apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.worker.configmapName }}
  namespace: {{ .Values.namespaceOverride | default .Releaes.Namespace }}
  labels:
  {{- include "node-feature-discovery.labels" . | nindent 4 }}
data:
  nfd-worker.conf: |-
    {{- .Values.worker.config | nindent 4 }}
