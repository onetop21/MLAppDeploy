!include schema-definitions.yaml

############################
# MLAppDeploy Project v0.2 #
############################
<<: *metadata
workspace: 
  type: dict
  selector: [*workspace, *dockerfile]
ingress:
  type: dict
  valuesrules: 
    type: dict
    schema: *ingress
app:
  type: dict
  valuesrules:
    type: dict
    selector: [*component]